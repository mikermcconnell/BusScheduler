# Manual Firebase Test Pages

These HTML utilities are retained for local testing only. They have been removed from the public build to avoid exposing credentials. Update the placeholder API keys with environment-specific values before use, and never commit real secrets here.
